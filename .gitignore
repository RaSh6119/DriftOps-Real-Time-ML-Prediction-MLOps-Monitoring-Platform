# Python
.venv/
__pycache__/
*.pyc

# ML artifacts — mostly regenerated locally, but a trained sample is
# committed intentionally so the live deployment has something to serve
# and reviewers can explore without running the full pipeline first.
data/raw/
data/*.csv
!data/train_processed.csv
!data/test_processed.csv
models/*
!models/latest_model.joblib
mlruns/

# Node / frontend
frontend/node_modules/
frontend/dist/

# env
.env
DECISIONS.md